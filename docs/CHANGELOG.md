# 改动日志

按时间倒序记录所有改动。设计文档见 [DESIGN.md](./DESIGN.md)。

---

## 2026-06-15 · 除AI味模块最终验证 — 朱雀检测人工特征100%

**目标**:绕过腾讯朱雀AI检测,让AI生成的小说文本通过检测。

**验证结果**:朱雀AI检测 → **人工特征 100%**,疑似AI 0%,AI特征 0%。

**生效方案**(回退到验证通过的版本):

### 1. AI生成提示词
- 默认续写模式:模拟"凌晨三点赶稿作者",要求句长极端不均+人味注入
- rewrite模式:针对朱雀检测8个维度优化(困惑度/突发性/语义结构/段首重复/比喻词密度/情感均匀/缺对话/缺碎片句)

### 2. 自动降AI味流程(开启⚙️后自动执行)
- 第1步:AI重写(1次API调用,temperature=1.5)
- 第2步:套话替换+长句拆短(postProcessText/splitLongSentences)
- 第3步:句式打散(scrambleSentenceStarts/scrambleParagraphs)
- 第4步:口语化注入(injectColloquial/injectFragments)
- 第5步:随机化打散(randomScramble)
- 第6步:人味注入(humanChaos — 思维跳跃/自相矛盾/具体细节/身体感受)

### 3. 其他降重模式
- 🔄 翻译链:中→日→英→中(3跳破坏token分布)
- 🧠 三轮降重协议:困惑度打散→语义结构打乱→风格注入
- 🔬 深度本地降重:51条AI高频词替换+句式打散+口语化注入
- 💥 终极降重:翻译链+三轮协议+人味注入最强组合
- ✨ 人工润色:用户写初稿,AI只做微调(temperature=0.3)

### 4. 朱雀检测绕过关键发现
- 朱雀看的是**困惑度(perplexity)**和**突发性(burstiness)**,不是表面用词
- "被降AI工具处理过的文本"会被额外标记——不能只做表面换词
- 翻译链通过跨语言翻译破坏token分布是有效方法之一
- 人工润色(用户写初稿+AI微调)保留人类统计特征最可靠
- 不要过度工程化——加太多规则反而制造新的可检测模式

---

## 2026-06-15 · 12.6 作品成品展示页

**目标**:为创作者提供一个"作品集"视角,集中浏览所有作品并支持在线阅读 + 一键导出。

**新增 UI**(顶栏)
- 📚 作品集按钮,唤起作品集弹窗(960×88vh)

**作品集弹窗**
- 工具栏:名称筛选(实时)+ 5 种排序(最近更新/创建/字数/完成度/名称)
- 汇总条:总作品数 + 累计字数 + 已完成章节数
- 网格卡片(每卡片 240px+,自动填充):
  - 渐变封面(8 种预设色按作品索引分配),含作品名 + 创建日期
  - 徽章:`当前` / `完结`(完成度=100%)
  - 统计:总字数 / 章节(完成/总数) / 关卡(完成/总数) / 更新时间
  - 完成度进度条 + 百分比
  - 操作按钮:📖 阅读 / ✏️ 编辑 / 📋 复制 / 🗑️ 删除
- 空状态:📭 提示 + "还没有任何作品"
- 底部:📤 导出全部作品(合并为单 HTML 下载)

**阅读模式**(子弹窗 960×92vh)
- 左侧目录(`reader-toc`):分组"设定"(世界观/主角/大纲) + "正文"(章节)
  - 每项显示关卡名 + 字数
  - 点击切换章节,激活项高亮
- 右侧内容(`reader-content`):章节标题(带 Boss 🔥 徽章)+ 渲染后内容
  - 使用 `mdToWechatHtml` 与公众号复制保持一致
  - 空内容显示"本关尚未创作内容"
- 底部:📋 复制全部(双格式)/ 📤 导出当前作品 HTML

**数据模型**
- `work.updatedAt` 字段(ISO 字符串),`scheduleSave()`/`saveNow()` 时自动更新
- `loadState()` 迁移老数据,补全 `updatedAt = createdAt`
- 新增辅助 `gatherWorkTextOf(w)` 接受任意 work(原 `gatherWorkText()` 只对当前作品)

**修复**(12.5 搜索)
- `searchInWork` 中 `forEach` 内 `break` 抛 `SyntaxError`,改为 `for...of` + `break outer`

**事件绑定**(避免再次发生 null 元素导致整个 `bind()` 中断)
- 顶栏 📚 → `showShowcase`
- 弹窗 ✕ / 关闭 / 背景点击 → `hideShowcase`
- 筛选 / 排序 → 实时重渲
- 导出全部 → 合并 + 触发下载
- 阅读弹窗 ✕ / 关闭 / 背景点击 → `hideReader`
- 复制 / 导出 → ClipboardItem + Blob 下载

**验证**
- 浏览器手工:多作品展示 / 排序 / 筛选 / 阅读 / 复制 / 导出 全部通过

---

## 2026-06-15 · 12.5 全文搜索 + DeepSeek API

**目标**:关键词在所有作品的所有章节中检索,Ctrl+Shift+F 唤起。

**新增**
- 顶栏 🔍 按钮,Ctrl+Shift+F 全局快捷键
- 搜索弹窗(720×85vh):输入框 + 范围(当前/全部)+ 结果区 + 历史 chip
- 关键词在结果中用 `<mark>` 高亮(黄色背景)
- 上下文片段 ±20 字,前后省略号
- 搜索历史(最多 10 条),点击 chip 重搜
- 跨作品上限 200 个结果
- 点击结果:自动切到对应作品/关卡 + 滚动 + 选中匹配

**DeepSeek 接入**(已完成,合并至此条目)
- 真实 `callAI` 调用 `https://api.deepseek.com/v1/chat/completions`
- 系统提示词:"你是一位才华横溢的中文小说家...续写 300-500 字"
- ⚙️ API Key 设置弹窗,Key 仅存 localStorage
- 未配置时点击 AI 按钮自动唤起设置

**修复**
- `bind()` 中 `btn-generate-title` 实际为 `title-hint` 导致 null,所有弹窗关闭按钮全部失效
- 改为精确选择 + null 检查,加新元素前先加保护

---

## 2026-06-13 · 文档对齐

## 2026-06-13 · 文档对齐

把设计文档和实现状态对齐:
- `docs/DESIGN.md` 加 "0. 实现状态总览" 表格,所有子功能加 ✅/🟡/⚪ 状态
- 各功能章节(1-9, 12.x)加"已实现/未开始"标记
- 新建本 `CHANGELOG.md` 记录具体改动

---

## 2026-06-13 · TODO 占位符系统 (设计 12.3)

**目标**:追踪写作时"还差什么"。

**新增**
- 状态栏顶部:`TODO N` 实时未完成数(橙色提示,N=0 时灰)
- 侧边栏底部:`📌 TODO(N)` 折叠面板,列出所有 TODO
  - 每项显示:TODO 文本、所在关卡(`第 X 章`)、字符位置
  - 橙色卡片样式区分于关卡
- 写作时输入 `[TODO: 描述]` 即创建占位符
- 公众号复制时 TODO 转为黄色高亮 `<span>`

**交互**
- 点击 TODO 项 → 切换到对应关卡 + 光标选中 TODO 文本
- 点击 TODO 前 □ → 标记完成(绿色 ✓ + 删除线 + 变灰)
- 顶部 `TODO` 可点击自动展开折叠面板
- 跨作品独立(每部作品维护自己的 `todoDone` 字典)

**技术**
- 每次 `render()` 重新扫描所有关卡内容,正则 `\[TODO:[^\]]*\]`
- 勾选状态存 `work.todoDone[lvlId + ':' + offset]`,按位置标识
- offset 用 `match.index` 取字符位置,文本编辑后失效(自动重新扫描)

**验证**(Node 端 + 浏览器 Playwright)
- 扫描 + 计数 + 跳转 + 勾选 + 跨作品独立,5 项断言全过,无 pageerror

---

## 2026-06-13 · 公众号格式一键复制 (设计 12.7)

**目标**:在工具里写完,直接复制粘贴到微信公众号编辑器,样式不丢。

**UI**
- 动作栏新增 "📋 复制公众号格式" 主按钮 + "▾" 下拉
- 下拉选项:`📄 复制当前章节` / `📚 复制整部作品` / `👁 预览 HTML` / `🔖 复制 HTML 源码`
- Toast 提示(2.2s 自动消失,成功绿/失败红)
- 预览弹窗(720×86vh,深色背景,白底渲染):
  - 双 tab:`渲染效果` / `HTML 源码`
  - 内置"复制到剪贴板" + "关闭"按钮
  - ESC 键关闭,点遮罩关闭

**转换规则**(Markdown → 公众号兼容 HTML)
| Markdown | 输出 |
|----------|------|
| 段落(空行分隔) | `<p style="margin:0 0 14px;line-height:1.85;font-size:16px;color:#333;">` |
| `# 一级标题` | `<p style="font-size:1.4em;font-weight:bold;...">` |
| `## 二级标题` | `<p style="font-size:1.2em;font-weight:bold;...">` |
| `**粗体**` | `<strong style="color:#000;font-weight:bold;">` |
| `*斜体*` | `<em style="font-style:italic;color:#555;">` |
| `> 引用` | `<blockquote style="border-left:3px solid #ccc;...">` |
| `[TODO: x]` | `<span style="background:#fff3a0;color:#7a5a00;">` |
| 段间 | `<p><br></p>` 间隔 |

**整部作品复制**
- 按关卡顺序拼接,每个关卡加标题:
  - 世界观 → `# 世界观`
  - 主角 → `# 主角`
  - 大纲 → `# 大纲`
  - 章节 → `# 第N章 · 第X关`
- 关卡之间用 `\n\n---\n\n` 分隔

**技术**
- 优先用 `navigator.clipboard.writeText`(现代 API)
- 降级到 `document.execCommand("copy")` + 临时 textarea
- 内联样式让公众号粘贴后保留(不用 `<style>`,公众号会剥)

**修复**
- 重写整个 `app.js`(原 patch 累积了多处 syntax error)
- 修复了 `function inspireHint() {` 被 patch 误删的 bug

**验证**
- 浏览器 Playwright:toast 弹出 ✓,预览渲染正确 ✓,HTML 源码含全部样式 ✓
- 注:Edge headless 模式下读不到剪贴板内容(权限),不影响实际用户使用

---

## 2026-06-13 · 多作品管理(架构升级)

**目标**:支持同时创作多部小说,互不干扰。

**架构改动**
- `state` 从单一作品改为:
  ```js
  state = { activeWorkId, works: { [id]: {...} } }
  ```
- 存档键升级 `novel-quest.v1` → `novel-quest.v2`(老数据自动迁移)
- 每个作品独立维护:名字、创建时间、当前关卡、33 关内容、TODO 勾选状态

**UI**
- 顶栏新增"作品切换器"下拉(`▾`),点开显示:
  - 所有作品列表(名称 + 字数 + 完成关卡数)
  - 当前作品打 ✓
  - 底部"＋ 新建作品"
- 侧边栏底部新增"✎ 重命名作品"和"🗑 删除当前作品"按钮

**功能**
- `createWork(name)`:创建新作品,自动切到新作品
- `renameWork(id)`:弹 prompt 重命名
- `deleteWork(id)`:弹 confirm,至少保留 1 部
- `switchWork(id)`:切到指定作品,所有视图自动重渲

**数据迁移**
- 加载时检测 `novel-quest.v2` 优先,否则尝试 `novel-quest.v1` 迁移
- 老数据变成一部"我的旧作"作品

**验证**
- Node 端 22 个 smoke test,含多作品隔离、字数独立、持久化、老 v1 迁移、删除
- 全部通过

---

## 2026-06-10 · 初始骨架

**目标**:让 DESIGN.md 的设计变成可运行的代码。

**P0 功能**
- 单 `index.html`,无依赖,无构建
- 关卡定义 33 关(1 世界观 + 1 主角 + 1 大纲 + 30 章节,Boss 关在第 10/20/30 章)
- 侧边栏关卡列表(33 项,带状态:未解锁/可玩/已完成)
- 主区:目标说明 + textarea 编辑器 + 4 个动作按钮(AI 生成/灵感/提交通关/重置)
- 底部状态栏:字数/累计/总关卡/保存状态/存储用量
- localStorage 自动存档(300ms 防抖)
- 关卡解锁/锁定/通关逻辑
- 字数门槛递增
- 暗色主题

**P1**
- AI 生成 mock(返回固定示例文本,接口 `callAI()` 预留)
- 灵感按钮(按关卡类型返回不同提示)
- Ctrl+S 手动保存
- 视觉打磨(Boss 关金色边框、关卡通关变绿)

**技术决策**
- 纯原生 HTML/CSS/JS,文件总计 ~600 行
- 样式用 CSS 变量统一主题色
- 不引第三方库(包括不引 jQuery、不用框架)

---

## 未来规划

待实现功能(按推荐顺序):

1. ~~**12.4 写作日历**~~ — ✅ 已完成
2. ~~**12.6 作品成品展示页**~~ — ✅ 已完成
3. ~~**12.12 公众号标题生成器**~~ — ✅ 已完成
4. ~~**12.5 全文搜索**~~ — ✅ 已完成
5. ~~**12.11 付费章节标注**~~ — ✅ 已完成
6. ~~**12.10 写作实时反馈**~~ — ✅ 已完成
7. ~~**9. 除 AI 味 / 降重**~~ — ✅ 已完成(朱雀检测人工特征100%已验证)
8. ~~**真 AI 接入(DeepSeek)**~~ — ✅ 已完成
